

var math = {
  max: function(n1, n2) {
    return Math.max(n1, n2)
  },
  len: function(arr) {
    arr = arr || []
    return arr.length
  },
  logicOr:function(a,b){

    return a||b
  }
}

module.exports = {
  max: math.max,
  len: math.len,
  logicOr: math.logicOr
}